import React from "react";
import { Button } from "@livechat/design-system";

const Details = () => {
  const handleCreateCustomer = () => {};

  return <Button onClick={handleCreateCustomer} />;
};

export default Details;
